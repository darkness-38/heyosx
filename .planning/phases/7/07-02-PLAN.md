---
phase: 07-polish
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified: [heydm/src/window.rs, heydm/src/render.rs]
autonomous: true
requirements: [FEAT-POLISH-02]

must_haves:
  truths:
    - "Workspace switching triggers a smooth horizontal sliding animation"
    - "Windows from the previous workspace slide out while new ones slide in"
  artifacts:
    - path: "heydm/src/window.rs"
      provides: "Animation state for workspace transitions"
    - path: "heydm/src/render.rs"
      provides: "Offset rendering logic for workspace animation"
---

<objective>
Implement a smooth sliding animation for workspace transitions to provide premium visual feedback.

Purpose: Enhance the "fluid" feel of the desktop environment.
Output: Animated horizontal sliding between workspaces.
</objective>

<execution_context>
@C:/Users/hamza/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/hamza/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/7/07-01-SUMMARY.md
@heydm/src/window.rs
@heydm/src/render.rs
</context>

<interfaces>
From heydm/src/window.rs:
```rust
pub struct WindowManager {
    // Added in 07-01:
    pub current_workspace: usize,
    // ...
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add workspace animation state to window.rs</name>
  <files>heydm/src/window.rs</files>
  <action>
    - Add `pub current_workspace_offset: f64` to `WindowManager` (init to 0.0).
    - Update `WindowManager::update_animations`:
      - Interpolate `current_workspace_offset` towards `self.current_workspace as f64` using the same easing factor (12.0) and `dt`.
    - Modify `WindowManager::windows()` to return ALL windows when `current_workspace_offset != current_workspace as f64` (to ensure both incoming and outgoing windows are rendered during transition).
  </action>
  <verify>
    <automated>cargo check</automated>
  </verify>
  <done>WindowManager now tracks a continuous offset for workspace position.</done>
</task>

<task type="auto">
  <name>Task 2: Apply workspace offset in Renderer</name>
  <files>heydm/src/render.rs</files>
  <action>
    - Update `Renderer::gather_elements`:
      - For each window, calculate `ws_offset_x = (window.workspace as f64 - state.window_manager.current_workspace_offset) * state.output_size.w as f64`.
      - Apply `ws_offset_x` to the window's `location` before rendering elements.
    - Update `Renderer::draw_ui`:
      - Similarly, apply the same horizontal offset when drawing window decorations (shadows/borders).
      - Ensure windows that are completely off-screen (e.g. `abs(ws_offset) > 1.0`) are skipped for performance.
  </action>
  <verify>
    <automated>cargo check</automated>
  </verify>
  <done>Rendering logic applies workspace offsets, enabling the horizontal sliding effect.</done>
</task>

</tasks>

<verification>
1. `cargo check` passes.
2. Run `heydm` and switch between workspaces with windows.
3. Verify that the windows slide horizontally from/to the correct direction.
</verification>

<success_criteria>
- Workspace switching is no longer instantaneous; it has a smooth sliding animation.
- Both current and destination workspace windows are visible during the transition.
</success_criteria>

<output>
After completion, create `.planning/phases/07-polish/07-02-SUMMARY.md`
</output>
