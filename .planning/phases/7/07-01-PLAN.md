---
phase: 07-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [heydm/src/window.rs, heydm/src/input.rs]
autonomous: true
requirements: [FEAT-POLISH-01]

must_haves:
  truths:
    - "User can switch between 9 workspaces using Super+1-9"
    - "User can move the focused window to another workspace using Super+Shift+1-9"
    - "Only windows on the current workspace are rendered"
  artifacts:
    - path: "heydm/src/window.rs"
      provides: "Workspace state tracking in WindowElement and WindowManager"
    - path: "heydm/src/input.rs"
      provides: "Keybindings for workspace switching and moving windows"
  key_links:
    - from: "heydm/src/input.rs"
      to: "heydm/src/window.rs"
      via: "WindowManager::switch_workspace and WindowManager::move_focused_to_workspace"
---

<objective>
Enable multi-workspace support in the compositor and implement keybindings for switching and moving windows.

Purpose: Foundation for workspace-based organization and navigation.
Output: Working multi-workspace system with basic input control.
</objective>

<execution_context>
@C:/Users/hamza/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/hamza/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@heydm/src/window.rs
@heydm/src/input.rs
</context>

<interfaces>
From heydm/src/window.rs:
```rust
pub struct WindowElement {
    toplevel: ToplevelSurface,
    position: Point<i32, Logical>,
    size: Size<i32, Logical>,
    pub current_position: Point<f64, Logical>,
    pub current_size: Size<f64, Logical>,
    // ...
}

pub struct WindowManager {
    windows: Vec<WindowElement>,
    focused: Option<usize>,
    pub layout: Layout,
    // ...
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add workspace state to window.rs</name>
  <files>heydm/src/window.rs</files>
  <action>
    - Add `workspace: usize` to `WindowElement` (default to 0 in `new()`).
    - Add `current_workspace: usize` to `WindowManager` (initialize to 0 in `new()`).
    - Implement `WindowManager::switch_workspace(new_workspace: usize)`:
      - Updates `current_workspace`.
      - Updates focus if the focused window is no longer visible.
      - Calls `recompute_layout`.
    - Implement `WindowManager::move_focused_to_workspace(target_workspace: usize)`:
      - Moves the focused window to the specified workspace.
      - Updates focus to the next available window on the current workspace.
      - Calls `recompute_layout`.
    - Modify `WindowManager::windows()` (or add `visible_windows()`) to return windows only on the `current_workspace`. 
    - Note: Ensure `recompute_layout` only tiles windows on the current workspace.
  </action>
  <verify>
    <automated>cargo check</automated>
  </verify>
  <done>Window and Manager structs updated with workspace awareness; methods for switching and moving implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Implement workspace keybindings in input.rs</name>
  <files>heydm/src/input.rs</files>
  <action>
    - Add `SwitchWorkspace(usize)` and `MoveWindowToWorkspace(usize)` variants to `CompositorAction`.
    - In `check_compositor_binding`:
      - Map Super+1-9 (Keysyms `_1` to `_9`) to `SwitchWorkspace(idx)`.
      - Map Super+Shift+1-9 to `MoveWindowToWorkspace(idx)`.
    - In `execute_action`:
      - Handle `SwitchWorkspace(n)` by calling `state.window_manager.switch_workspace(n)`.
      - Handle `MoveWindowToWorkspace(n)` by calling `state.window_manager.move_focused_to_workspace(n)`.
  </action>
  <verify>
    <automated>cargo check</automated>
  </verify>
  <done>Keybindings for workspaces 1-9 are active and mapped to WindowManager actions.</done>
</task>

<task type="auto">
  <name>Task 3: Refine visibility and tiling for workspaces</name>
  <files>heydm/src/window.rs</files>
  <action>
    - Update `WindowManager::recompute_layout` to only process windows where `w.workspace == self.current_workspace`.
    - Update `WindowManager::surface_under` and `WindowManager::focus_at` to only consider windows on the current workspace.
    - Ensure `WindowManager::windows()` returns only windows on `current_workspace` so `Renderer` automatically filters them.
  </action>
  <verify>
    <automated>cargo check</automated>
  </verify>
  <done>Compositor interactions (rendering, hit-testing, tiling) are strictly confined to the active workspace.</done>
</task>

</tasks>

<verification>
1. `cargo check` passes.
2. Run `heydm` and verify opening windows on workspace 0, switching to workspace 1 (windows disappear), then back to 0 (windows reappear).
3. Verify moving a window to workspace 2 removes it from current view.
</verification>

<success_criteria>
- Workspace state is correctly maintained in `WindowManager`.
- Keyboard shortcuts for 9 workspaces are functional.
- Windows are strictly isolated between workspaces in both rendering and interaction.
</success_criteria>

<output>
After completion, create `.planning/phases/07-polish/07-01-SUMMARY.md`
</output>
