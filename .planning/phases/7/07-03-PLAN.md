---
phase: 07-polish
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified: [heydm/src/window.rs, heydm/src/render.rs]
autonomous: true
requirements: [FEAT-POLISH-03]

must_haves:
  truths:
    - "An animated focus indicator box follows the focused window"
    - "The indicator moves independently with its own easing for a 'magnetic' feel"
  artifacts:
    - path: "heydm/src/window.rs"
      provides: "Animated state for the focus indicator"
    - path: "heydm/src/render.rs"
      provides: "Rendering logic for the high-fidelity focus glow/box"
---

<objective>
Implement a premium animated focus indicator that provides smooth, "magnetic" visual feedback by following the focused window.

Purpose: Professional-grade interaction feedback similar to end-4 hyprland.
Output: Dynamic, animated focus box/glow.
</objective>

<execution_context>
@C:/Users/hamza/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/hamza/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/7/07-02-SUMMARY.md
@heydm/src/window.rs
@heydm/src/render.rs
</context>

<interfaces>
From heydm/src/window.rs:
```rust
pub struct WindowManager {
    focused: Option<usize>,
    // ...
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add focus indicator state to window.rs</name>
  <files>heydm/src/window.rs</files>
  <action>
    - Add `pub current_focus_pos: Point<f64, Logical>` and `pub current_focus_size: Size<f64, Logical>` to `WindowManager`.
    - Add `pub focus_opacity: f32` to `WindowManager` (init to 0.0).
    - Update `WindowManager::update_animations`:
      - If `self.focused` is `Some(idx)`:
        - Target `pos` and `size` are from `self.windows[idx].position` and `size` (not current animated ones, so it "leads" or follows target).
        - Interpolate `current_focus_pos` and `current_focus_size` towards target using a slightly different easing factor (e.g. 10.0 instead of 12.0) for a "loose/magnetic" feel.
        - Interpolate `focus_opacity` towards 1.0.
      - If `self.focused` is `None`:
        - Interpolate `focus_opacity` towards 0.0.
  </action>
  <verify>
    <automated>cargo check</automated>
  </verify>
  <done>WindowManager now tracks the independent animated geometry of the focus indicator.</done>
</task>

<task type="auto">
  <name>Task 2: Render focus indicator in Renderer</name>
  <files>heydm/src/render.rs</files>
  <action>
    - Update `Renderer::draw_ui`:
      - Draw a specialized "Focus Box" using `tiny-skia` based on `state.window_manager.current_focus_pos` and `size`.
      - Use `focus_opacity` for transparency.
      - Design: A thin, vibrant border (e.g. #BD93F9) or a subtle outer glow.
      - Ensure the workspace offset from Plan 07-02 is NOT applied here, or rather, ensure it follows the *currently visible* position if preferred (actually, it's better if it follows the workspace sliding too).
      - Applying workspace offset: Calculate `ws_offset_x` for the focused window's workspace and apply it to the focus box rendering.
  </action>
  <verify>
    <automated>cargo check</automated>
  </verify>
  <done>The focus indicator is rendered and animates between windows when focus changes.</done>
</task>

</tasks>

<verification>
1. `cargo check` passes.
2. Run `heydm` and open multiple windows.
3. Switch focus between windows and verify the focus box "flies" or "slides" to the new target.
4. Verify the indicator fades out when no window is focused.
</verification>

<success_criteria>
- Focus indicator is visually distinct and modern.
- Animation is smooth and feels responsive.
- Indicator correctly handles workspace transitions.
</success_criteria>

<output>
After completion, create `.planning/phases/07-polish/07-03-SUMMARY.md`
</output>
