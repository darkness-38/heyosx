---
phase: 04-gles2-rendering-shaders
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/monet.h, src/monet.c, src/render.h, src/render.c, src/main.c, Makefile]
autonomous: true
requirements: [FR-4.4]
must_haves:
  truths:
    - "Monet state management exists and can store/provide UI colors"
    - "Shader infrastructure can load and compile GLES2 shaders"
    - "Custom render handler is hooked into the wlroots scene output"
  artifacts:
    - path: "src/monet.c"
      provides: "Color state management"
    - path: "src/render.c"
      provides: "GLES2 shader infrastructure and scene render hook"
  key_links:
    - from: "src/main.c"
      to: "src/render.c"
      via: "output_frame_handler calling custom render"
---

<objective>
Establish the foundation for custom GLES2 rendering and implement the Monet dynamic theming state.

Purpose: Provide the infrastructure needed for subsequent visual effects (SDF corners, shadows, blur) and manage the system's color palette.
Output: Monet state module, shader compiler utility, and a hooked custom render loop.
</objective>

<execution_context>
@C:/Users/hamza/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/hamza/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/main.c
@src/server.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Monet dynamic theming state</name>
  <files>src/monet.h, src/monet.c</files>
  <action>
    Create a lightweight module to manage the "end-4" color palette (Monet/Material You).
    - Define `struct heyde_colors` containing fields like `accent`, `background`, `surface`, `on_surface`.
    - Implement `heyde_monet_init()` to set default colors.
    - Implement `heyde_monet_get_colors(struct heyde_server *server)` to retrieve the current palette.
    - Add the Monet state to `struct heyde_server` in `server.h`.
  </action>
  <verify>
    <automated>gcc -c src/monet.c -I. `pkg-config --cflags wlroots-0.18` -o /dev/null</automated>
  </verify>
  <done>Monet state module exists and is integrated into the server struct.</done>
</task>

<task type="auto">
  <name>Task 2: Setup GLES2 Shader Infrastructure</name>
  <files>src/render.h, src/render.c, Makefile</files>
  <action>
    Implement utilities for loading and compiling GLES2 shaders.
    - Update `Makefile` to include `glesv2` in `LIBS` and `CFLAGS`.
    - Create `heyde_render_init_shaders()` to compile a basic vertex and fragment shader.
    - Implement `heyde_render_output_handler()` which will be used with `wlr_scene_output_set_render_handler`.
    - For now, the custom render handler should just call `wlr_scene_output_render` but prepare the GLES2 context.
  </action>
  <verify>
    <automated>make clean && make heyde</automated>
  </verify>
  <done>GLES2 shaders can be compiled and the custom render handler is ready for implementation.</done>
</task>

<task type="auto">
  <name>Task 3: Hook Custom Render Handler</name>
  <files>src/main.c</files>
  <action>
    Replace the default scene rendering with the custom handler.
    - In `server_new_output_handler`, use `wlr_scene_output_set_render_handler` to point to the new handler in `src/render.c`.
    - Update `output_frame_handler` to trigger the custom render process if necessary, or let the handler handle the commit.
    - Ensure `wlr_renderer_begin` and `wlr_renderer_end` are handled correctly within the custom pass.
  </action>
  <verify>
    <automated>make heyde && ./heyde --help (or equivalent to check it runs without crashing)</automated>
  </verify>
  <done>Compositor uses the custom GLES2 render handler for output frames.</done>
</task>

</tasks>

<verification>
Check that the compositor still starts and renders windows normally using the custom handler (which currently just delegates to the standard scene render).
</verification>

<success_criteria>
- Monet state is initialized with default "end-4" colors.
- `heyde` binary links against GLESv2.
- Custom render handler is called every frame.
</success_criteria>

<output>
After completion, create `.planning/phases/04-gles2-rendering-shaders/04-01-SUMMARY.md`
</output>
