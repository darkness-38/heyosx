---
phase: 04-gles2-rendering-shaders
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified: [src/shaders/blur.frag, src/render.c, src/render.h]
autonomous: true
requirements: [FR-4.3]
must_haves:
  truths:
    - "Multi-pass Kawase blur is implemented and functional"
    - "Blur is applied to background or specified layers"
    - "Performance remains stable during blur operations"
  artifacts:
    - path: "src/shaders/blur.frag"
      provides: "Kawase blur fragment shader"
  key_links:
    - from: "src/render.c"
      to: "src/shaders/blur.frag"
      via: "Multi-pass framebuffer objects"
---

<objective>
Implement multi-pass Kawase blur for desktop UI and window background effects.

Purpose: Provide the signature "end-4" glassmorphic look through high-performance blurring.
Output: Kawase blur shader, framebuffer management for multiple passes, and integration into the render loop.
</objective>

<execution_context>
@C:/Users/hamza/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/hamza/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/render.c
@src/render.h
@.planning/phases/04-gles2-rendering-shaders/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Kawase Blur Algorithm</name>
  <files>src/shaders/blur.frag, src/render.c</files>
  <action>
    Create a fragment shader for the Kawase blur pass.
    - Multi-pass approach: downsample, apply blur passes, then upsample.
    - Implement the downsampling/upsampling logic in `src/render.c`.
    - Set up the necessary GLES2 framebuffers (FBOs) to handle the intermediate passes.
  </action>
  <verify>
    <automated>gcc -c src/render.c -I. `pkg-config --cflags wlroots-0.18` -o /dev/null</automated>
  </verify>
  <done>Kawase blur shader and multi-pass infrastructure are implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Apply Blur to Background Layers</name>
  <files>src/render.c</files>
  <action>
    Integrate the blur effect into the custom render loop.
    - Define which layers or surface types should trigger blurring (e.g., background behind bars/docks).
    - Ensure blur is only applied where needed to maintain performance.
    - Hook into the custom render handler to perform the blur passes before drawing the foreground elements.
  </action>
  <verify>
    <automated>make heyde</automated>
  </verify>
  <done>Blur is applied to the background layers, creating the glassmorphic effect.</done>
</task>

</tasks>

<verification>
Check for blur behind UI elements like layer-shell surfaces. Ensure no performance degradation.
</verification>

<success_criteria>
- Blur shader compiles successfully.
- FBOs are correctly managed and cleaned up.
- Visible blur effect on background layers.
</success_criteria>

<output>
After completion, create `.planning/phases/04-gles2-rendering-shaders/04-03-SUMMARY.md`
</output>
