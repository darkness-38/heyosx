---
phase: 04-gles2-rendering-shaders
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified: [src/shaders/corner.frag, src/shaders/shadow.frag, src/render.c, src/render.h]
autonomous: true
requirements: [FR-4.1, FR-4.2]
must_haves:
  truths:
    - "Windows render with 12px rounded corners"
    - "Corners are antialiased via SDF"
    - "Toplevel windows have soft Gaussian shadows"
  artifacts:
    - path: "src/shaders/corner.frag"
      provides: "SDF rounded corner fragment shader"
    - path: "src/shaders/shadow.frag"
      provides: "Gaussian shadow fragment shader"
  key_links:
    - from: "src/render.c"
      to: "src/shaders/corner.frag"
      via: "GLES2 shader program"
---

<objective>
Implement SDF-based rounded corners (12px) and soft Gaussian shadows for toplevel windows.

Purpose: Achieve the core "end-4" visual aesthetic with high-quality antialiasing and depth.
Output: Custom fragment shaders for corners and shadows, integrated into the custom rendering handler.
</objective>

<execution_context>
@C:/Users/hamza/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/hamza/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/render.c
@src/render.h
@.planning/phases/04-gles2-rendering-shaders/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement SDF Rounded Corners Shader</name>
  <files>src/shaders/corner.frag, src/render.c</files>
  <action>
    Create a fragment shader that uses Signed Distance Fields (SDF) to render rounded corners.
    - Radius: 12px.
    - Implement antialiasing using `smoothstep` on the distance field.
    - Integrate the shader into `src/render.c`.
    - Modify the render handler to apply this shader when drawing scene buffers (windows).
  </action>
  <verify>
    <automated>gcc -c src/render.c -I. `pkg-config --cflags wlroots-0.18` -o /dev/null</automated>
  </verify>
  <done>SDF shader is implemented and applied to window surfaces.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Gaussian Shadow Rendering</name>
  <files>src/shaders/shadow.frag, src/render.c</files>
  <action>
    Create a fragment shader for rendering soft shadows.
    - Radius: 20px (per FR-4.2).
    - Shadow should be rendered slightly offset and behind the main surface.
    - Optimize by drawing a larger rectangle behind the window node.
    - Use the Monet surface color for the shadow (attenuated).
  </action>
  <verify>
    <automated>make heyde</automated>
  </verify>
  <done>Soft Gaussian shadows are rendered behind toplevel windows.</done>
</task>

</tasks>

<verification>
Run the compositor and verify (visually if possible, or check logs for shader compilation errors) that windows have rounded corners and shadows.
</verification>

<success_criteria>
- Shader compilation succeeds.
- Window corners are visually rounded.
- Soft shadows are visible behind focused and unfocused toplevels.
</success_criteria>

<output>
After completion, create `.planning/phases/04-gles2-rendering-shaders/04-02-SUMMARY.md`
</output>
