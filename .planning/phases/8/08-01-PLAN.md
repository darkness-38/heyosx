---
phase: 08-rendering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [heydm/src/render.rs, heydm/src/input.rs, heydm/src/state.rs]
autonomous: true
requirements: [FIX-RENDERING-01, FIX-CURSOR-01]

must_haves:
  truths:
    - "The UI texture is visible on screen (no more black screen)"
    - "The cursor dot follows the pointer accurately (not inverted)"
  artifacts:
    - path: "heydm/src/render.rs"
      provides: "Corrected rendering logic for UI texture and cursor"
    - path: "heydm/src/input.rs"
      provides: "Corrected cursor position tracking"
---

<objective>
Fix the rendering pipeline to eliminate the black screen and correct the inverted cursor motion.

Purpose: Restore visibility and basic usability to heydm.
Output: Working rendering and accurate input tracking.
</objective>

<execution_context>
@C:/Users/hamza/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/hamza/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@heydm/src/render.rs
@heydm/src/input.rs
@heydm/src/state.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix UI Texture Rendering (Black Screen)</name>
  <files>heydm/src/render.rs, heydm/src/state.rs</files>
  <action>
    - In `state.rs`, ensure `ui_texture` import uses correct format. `tiny-skia` is RGBA, try `Fourcc::Rgba8888` if `Abgr8888` fails or produces black.
    - In `render.rs`, simplify `render_texture_from_to` call. The source rectangle for a 1:1 buffer-to-physical mapping should match the buffer size.
    - Add `tracing::debug!` or `info!` calls to verify texture creation and frame start.
    - Change `frame.clear` color from black `[0.0, 0.0, 0.0, 1.0]` to something distinct (e.g., deep purple `[0.1, 0.0, 0.2, 1.0]`) to confirm `clear` itself works.
  </action>
  <verify>
    <automated>cargo check</automated>
  </verify>
  <done>The background is visible and not black.</done>
</task>

<task type="auto">
  <name>Task 2: Fix Cursor Inversion</name>
  <files>heydm/src/input.rs, heydm/src/window.rs</files>
  <action>
    - Investigate `update_cursor_relative` and `set_cursor_position` in `window.rs`.
    - In `input.rs`, verify `x_transformed` and `y_transformed`. If winit coordinates are inverted, flip them.
    - Ensure the "white dot" in `render.rs` is drawn at the correct coordinates.
  </action>
  <verify>
    <automated>cargo check</automated>
  </verify>
  <done>The cursor dot tracks the mouse correctly.</done>
</task>

</tasks>

<verification>
1. `cargo check` passes.
2. Build and run heydm.
3. Verify the screen shows the dark gray background from `draw_ui`.
4. Verify the white dot follows the pointer precisely.
</verification>

<success_criteria>
- Black screen resolved.
- Cursor motion direction corrected.
- Basic compositor shell is visible.
</success_criteria>

<output>
After completion, create `.planning/phases/8/08-01-SUMMARY.md`
</output>
