---
phase: 08-core-fixes-black-screen-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/main.c]
autonomous: true
requirements: [Task 8.1, Task 8.3]

must_haves:
  truths:
    - "Compositor shows a solid color background instead of a black screen"
    - "WLR_DEBUG logs show when XDG surfaces are created and mapped"
  artifacts:
    - path: "src/main.c"
      provides: "Solid background and debug logging"
  key_links:
    - from: "src/main.c"
      to: "wlr_scene_rect_create"
      via: "API call"
---

<objective>
Provide a solid color background to fix the black screen issue and add debug logging to track surface mapping lifecycle.
</objective>

<execution_context>
@C:/Users/hamza/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/hamza/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/main.c
@src/monet.c
@src/monet.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add solid background to main.c</name>
  <files>src/main.c</files>
  <action>
    - In `main.c`, after `server.scene = wlr_scene_create();` and layer initialization:
    - Obtain the layout box using `wlr_output_layout_get_box(server.output_layout, NULL, &layout_box)`.
    - Create a solid color rect using `wlr_scene_rect_create(server.layers[ZWLR_LAYER_SHELL_V1_LAYER_BACKGROUND], layout_box.width, layout_box.height, color)`.
    - Use the background color from `server.monet_colors->background`.
    - Note: Need to convert float[4] colors to uint32_t RGBA for `wlr_scene_rect_create`.
  </action>
  <verify>
    <automated>grep "wlr_scene_rect_create" src/main.c</automated>
  </verify>
  <done>Compositor creates a solid background rect in the background layer.</done>
</task>

<task type="auto">
  <name>Task 2: Add debug logging to surface handlers</name>
  <files>src/main.c</files>
  <action>
    - Add `wlr_log(WLR_DEBUG, "New XDG surface: %s", xdg_surface->toplevel->title);` to `server_new_xdg_surface_handler`.
    - Add `wlr_log(WLR_DEBUG, "Toplevel mapped: %s", toplevel->xdg_toplevel->title);` to `toplevel_map_handler`.
    - Ensure title is checked for NULL before logging.
  </action>
  <verify>
    <automated>grep "wlr_log(WLR_DEBUG" src/main.c</automated>
  </verify>
  <done>Debug logs are added to track surface mapping.</done>
</task>

</tasks>

<verification>
Check if `src/main.c` contains the new background creation and logging.
</verification>

<success_criteria>
Compositor initializes with a visible background and logs surface events.
</success_criteria>

<output>
After completion, create `.planning/phases/08-core-fixes-black-screen-shell/08-01-SUMMARY.md`
</output>
