---
phase: 08-core-fixes-black-screen-shell
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified: [src/server.h, src/main.c, src/workspace.c]
autonomous: true
requirements: [Task 8.2]

must_haves:
  truths:
    - "Workspaces are layered correctly (above Bottom panels, below Top panels)"
  artifacts:
    - path: "src/server.h"
      provides: "Updated server struct for new workspace layer"
    - path: "src/main.c"
      provides: "New layer ordering"
    - path: "src/workspace.c"
      provides: "Workspace attachment to dedicated layer"
  key_links:
    - from: "src/main.c"
      to: "wlr_scene_tree_create"
      via: "Order of creation determines Z-order"
---

<objective>
Re-organize scene graph layers to ensure workspaces (windows) are correctly ordered: Background -> Bottom -> Workspaces -> Top -> Overlay.
</objective>

<execution_context>
@C:/Users/hamza/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/hamza/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/server.h
@src/main.c
@src/workspace.c
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update server.h and main.c for layer re-ordering</name>
  <files>src/server.h, src/main.c</files>
  <action>
    - In `src/server.h`, add `struct wlr_scene_tree *workspace_layer;` to `struct heyde_server`.
    - In `src/main.c`, modify layer initialization to create trees in this exact order:
      1. `server.layers[ZWLR_LAYER_SHELL_V1_LAYER_BACKGROUND]`
      2. `server.layers[ZWLR_LAYER_SHELL_V1_LAYER_BOTTOM]`
      3. `server.workspace_layer`
      4. `server.layers[ZWLR_LAYER_SHELL_V1_LAYER_TOP]`
      5. `server.layers[ZWLR_LAYER_SHELL_V1_LAYER_OVERLAY]`
    - All should be created as children of `&server.scene->tree`.
  </action>
  <verify>
    <automated>grep "workspace_layer" src/server.h && grep "workspace_layer" src/main.c</automated>
  </verify>
  <done>Server struct and main initialization updated for new layer ordering.</done>
</task>

<task type="auto">
  <name>Task 2: Update workspace.c to use new workspace layer</name>
  <files>src/workspace.c</files>
  <action>
    - In `heyde_workspaces_init`, change `server->workspace_tree = wlr_scene_tree_create(server->layers[ZWLR_LAYER_SHELL_V1_LAYER_TOP]);` to use `server->workspace_layer` instead.
    - So it becomes `server->workspace_tree = wlr_scene_tree_create(server->workspace_layer);`.
  </action>
  <verify>
    <automated>grep "server->workspace_layer" src/workspace.c</automated>
  </verify>
  <done>Workspaces are now attached to the correct layer in the scene graph.</done>
</task>

</tasks>

<verification>
Check if `src/workspace.c` now uses `workspace_layer` and that the creation order in `src/main.c` is correct.
</verification>

<success_criteria>
Workspaces are rendered between the Bottom and Top layer-shell surfaces.
</success_criteria>

<output>
After completion, create `.planning/phases/08-core-fixes-black-screen-shell/08-02-SUMMARY.md`
</output>
