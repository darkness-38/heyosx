---
phase: 06-tiling
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified: [heydm/src/input.rs, heydm/src/window.rs]
autonomous: true
requirements: [FEAT-TILING-03]

must_haves:
  truths:
    - "User can toggle layout with Super+Space"
    - "User can increment master count with Super+I"
    - "User can decrement master count with Super+O"
    - "User can increment master ratio with Super+Period"
    - "User can decrement master ratio with Super+Comma"
  artifacts:
    - path: "heydm/src/input.rs"
      provides: "Tiling control keybindings"
      contains: "CompositorAction::ToggleLayout"
  key_links:
    - from: "InputHandler::execute_action"
      to: "WindowManager::toggle_layout"
      via: "method call"
---

<objective>
Expose tiling controls to the user via keyboard shortcuts. This plan enables interaction with the new tiling system.
</objective>

<execution_context>
@C:/Users/hamza/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/hamza/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@heydm/src/input.rs
@heydm/src/window.rs
@.planning/phases/6/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tiling actions and keybindings</name>
  <files>heydm/src/input.rs</files>
  <action>
    - Update `CompositorAction` enum:
      - Add `ToggleLayout`, `IncMasterCount`, `DecMasterCount`, `IncMasterRatio`, `DecMasterRatio`.
    - Update `InputHandler::check_compositor_binding`:
      - Super + Space -> `ToggleLayout`
      - Super + i | I -> `IncMasterCount`
      - Super + o | O -> `DecMasterCount`
      - Super + Comma -> `DecMasterRatio`
      - Super + Period -> `IncMasterRatio`
  </action>
  <verify>
    <automated>cargo check -p heydm</automated>
  </verify>
  <done>Compositor actions and keybindings are defined.</done>
</task>

<task type="auto">
  <name>Task 2: Implement tiling actions in InputHandler</name>
  <files>heydm/src/input.rs</files>
  <action>
    - Update `InputHandler::execute_action`:
      - Implement the 5 new actions by calling the corresponding methods on `state.window_manager`.
      - Pass `&state.output_size` to these methods.
  </action>
  <verify>
    <automated>cargo check -p heydm</automated>
  </verify>
  <done>User can control tiling via keyboard shortcuts.</done>
</task>

<task type="auto">
  <name>Task 3: Final verification and cleanup</name>
  <files>heydm/src/window.rs, heydm/src/input.rs</files>
  <action>
    - Run `cargo check` to ensure everything is correct.
    - Remove the old `tile_left` and `tile_right` methods from `WindowManager` (and their references in `InputHandler`) as they are replaced by dynamic tiling.
    - Ensure all newly added methods have basic logging.
  </action>
  <verify>
    <automated>cargo check -p heydm</automated>
  </verify>
  <done>Codebase is clean and tiling is the primary layout engine.</done>
</task>

</tasks>

<success_criteria>
- All new keybindings work as intended.
- `heydm` compiles without warnings or errors.
- Manual tiling methods are replaced by dynamic tiling.
</success_criteria>

<output>
After completion, create `.planning/phases/06-tiling/06-03-SUMMARY.md`
</output>
