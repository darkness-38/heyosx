---
phase: 06-tiling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [heydm/src/window.rs]
autonomous: true
requirements: [FEAT-TILING-03]

must_haves:
  truths:
    - "WindowManager has a layout mode"
    - "New windows trigger a layout recomputation"
    - "Closing windows triggers a layout recomputation"
  artifacts:
    - path: "heydm/src/window.rs"
      provides: "Layout enum and core tiling fields"
      contains: "pub enum Layout"
  key_links:
    - from: "WindowManager::add_window"
      to: "WindowManager::recompute_layout"
      via: "method call"
    - from: "WindowManager::remove_window"
      to: "WindowManager::recompute_layout"
      via: "method call"
---

<objective>
Refactor `WindowManager` to support dynamic layout management. This plan establishes the infrastructure for tiling but preserves current floating behavior as the default.
</objective>

<execution_context>
@C:/Users/hamza/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/hamza/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@heydm/src/window.rs

<interfaces>
From heydm/src/window.rs:
```rust
pub struct WindowManager {
    windows: Vec<WindowElement>,
    focused: Option<usize>,
    cursor_pos: (f64, f64),
    grab: Option<GrabState>,
    panel_height: i32,
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Layout enum and WindowManager fields</name>
  <files>heydm/src/window.rs</files>
  <action>
    - Add `#[derive(Debug, Clone, Copy, PartialEq)] pub enum Layout { Floating, Tiling }` above `WindowManager`.
    - Update `WindowManager` struct to include:
      - `layout: Layout` (default to `Tiling`)
      - `master_count: usize` (default to 1)
      - `master_ratio: f32` (default to 0.5)
      - `gaps: i32` (default to 10)
    - Update `WindowManager::new()` to initialize these fields.
  </action>
  <verify>
    <automated>cargo check -p heydm</automated>
  </verify>
  <done>WindowManager struct includes tiling state fields.</done>
</task>

<task type="auto">
  <name>Task 2: Implement recompute_layout skeleton and integration</name>
  <files>heydm/src/window.rs</files>
  <action>
    - Add `pub fn recompute_layout(&mut self, output_size: &smithay::utils::Size<i32, smithay::utils::Physical>)` to `WindowManager`.
    - Implementation: 
      - If `self.layout == Layout::Floating`, do nothing (or center windows if they were just added).
      - If `self.layout == Layout::Tiling`, add a TODO for the master/stack logic.
    - Update `add_window`, `remove_window`, and `toggle_fullscreen` to call `self.recompute_layout(output_size)` at the end of their operations.
    - Note: In `add_window`, remove the manual centering logic as it will be handled by `recompute_layout`.
  </action>
  <verify>
    <automated>cargo check -p heydm</automated>
  </verify>
  <done>All window management operations trigger a layout recomputation.</done>
</task>

</tasks>

<success_criteria>
- `heydm` compiles without errors.
- `WindowManager` tracks layout state.
- Layout recomputation is called when windows are added or removed.
</success_criteria>

<output>
After completion, create `.planning/phases/06-tiling/06-01-SUMMARY.md`
</output>
