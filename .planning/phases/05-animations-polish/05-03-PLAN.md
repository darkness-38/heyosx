---
phase: 05-animations-polish
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified: [src/animation.c, src/workspace.c, src/render.c, src/main.c, src/server.h]
autonomous: true
requirements: [ANIM-01, GEST-02, NFR-01]

must_haves:
  truths:
    - "No memory growth observed during repeated workspace switches or window mapping/unmapping"
    - "Animations maintain 60+ FPS on target hardware"
    - "All window and workspace transitions are fluid and follow spring physics"
  artifacts:
    - path: "src/render.c"
      provides: "Refined render loop with optimal scene-graph usage"
  key_links:
    - from: "src/workspace.c"
      to: "src/animation.c"
      via: "guaranteed cleanup of animation user_data"
---

<objective>
Final stability and performance tuning for the animation system and rendering loop.

Purpose: Ensure the compositor is production-ready, leak-free, and performs optimally.
Output: Hardened source code and finalized transition constants.
</objective>

<execution_context>
@C:/Users/hamza/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/hamza/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/animation.c
@src/workspace.c
@src/render.c
@.planning/phases/05-animations-polish/05-01-SUMMARY.md
@.planning/phases/05-animations-polish/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Memory Leak Audit & Fixes</name>
  <files>src/animation.c, src/workspace.c, src/main.c</files>
  <action>
    Perform a manual audit of all dynamic allocations in the animation and workspace modules.
    - Ensure `heyde_animation_tick` always frees the `animation` struct after completion.
    - Verify that `workspace_animation_data` and other `user_data` are correctly freed via `on_complete` callbacks.
    - Add a `destroy` handler for toplevels to ensure active animations for that window are cancelled or handled to prevent dangling pointers.
    - Fix any identified leaks in `src/main.c` related to signal handlers or resource cleanup.
  </action>
  <verify>
    <automated>make</automated>
  </verify>
  <done>Compositor resource usage is stable over time during stress testing.</done>
</task>

<task type="auto">
  <name>Task 2: Render Loop & Damage Tracking Optimization</name>
  <files>src/render.c, src/main.c</files>
  <action>
    Optimize the frame loop to reduce CPU/GPU overhead.
    - Review `heyde_render_output`. Ensure it leverages `wlr_scene` damage tracking effectively.
    - Implement a 'dirty' flag check for animations so the scene graph is only updated when an animation is actually in flight.
  </action>
  <verify>
    <automated>make</automated>
  </verify>
  <done>Rendering is efficient with minimal unnecessary redraws.</done>
</task>

<task type="auto">
  <name>Task 3: Aesthetic Polish & Documentation</name>
  <files>src/animation.c, src/workspace.c, src/render.c, README.md</files>
  <action>
    Finalize the "end-4" look and feel.
    - Adjust spring constants (`tension`, `friction`) in `workspace.c` and `main.c` to perfectly match the desired snappy-yet-fluid feel.
    - Add comprehensive Doxygen-style comments to `animation.h` and `workspace.h`.
    - Update the top-level README.md with instructions on using the new gestures and keybindings.
    - Perform a final code cleanup (consistent indentation, variable naming).
  </action>
  <verify>
    <automated>make</automated>
  </verify>
  <done>Codebase is clean, documented, and the UI feels professionally polished.</done>
</task>

</tasks>

<verification>
1. Launch heyDE.
2. Open and close multiple windows rapidly.
3. Perform continuous workspace swipes.
4. Monitor memory usage with `top` or `ps`.
5. Visually confirm that all transitions remain smooth without dropped frames.
</verification>

<success_criteria>
- Zero memory leaks detected in core animation/workspace paths.
- Animation constants feel "natural" and physics-accurate.
- Codebase follows C11 project conventions and is fully documented.
</success_criteria>

<output>
After completion, create `.planning/phases/05-animations-polish/05-03-SUMMARY.md`
</output>
