---
phase: 05-animations-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/animation.h, src/animation.c, src/server.h, src/main.c]
autonomous: true
requirements: [ANIM-01]

must_haves:
  truths:
    - "Windows slide or pop into view using smooth motion"
    - "Animations are physics-driven, not linear or easing-only"
  artifacts:
    - path: "src/animation.h"
      provides: "Animation struct and spring solver interface"
    - path: "src/animation.c"
      provides: "Spring-damper physics implementation"
  key_links:
    - from: "src/main.c (output_frame_handler)"
      to: "src/animation.c (heyde_animation_tick)"
      via: "frame signal callback"
---

<objective>
Implement a core physics-based spring animation system and apply it to basic window transitions (mapping/unmapping).

Purpose: Provide the foundational "fluid" motion for the compositor.
Output: `src/animation.h/c` modules and integrated frame loop updates.
</objective>

<execution_context>
@C:/Users/hamza/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/hamza/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/server.h
@src/main.c

<interfaces>
From src/server.h:
```c
struct heyde_server {
    struct wl_display *wl_display;
    struct wlr_scene *scene;
    struct wl_list outputs;
    struct wl_list toplevels;
    // ...
};

struct heyde_toplevel {
    struct wl_list link;
    struct heyde_server *server;
    struct wlr_xdg_toplevel *xdg_toplevel;
    struct wlr_scene_tree *scene_tree;
    // ...
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Core Animation Engine</name>
  <files>src/animation.h, src/animation.c, Makefile</files>
  <action>
    Create a spring-damper physics solver.
    - `struct heyde_spring`: current, target, velocity, tension, friction.
    - `heyde_spring_tick(struct heyde_spring *spring, double dt)`: Update state using semi-implicit Euler or similar.
    - `heyde_animation_manager`: A global structure to track active animations (linked list or array).
    - Update Makefile to include new source files.
  </action>
  <verify>
    <automated>make && ./heyde --help</automated>
  </verify>
  <done>Animation engine compiles and is ready for integration.</done>
</task>

<task type="auto">
  <name>Task 2: Frame Loop Integration</name>
  <files>src/server.h, src/main.c</files>
  <action>
    Integrate the animation engine into the compositor's frame loop.
    - Add `struct heyde_animation_manager animations` to `heyde_server`.
    - In `output_frame_handler` (src/main.c), calculate `dt` (time delta since last frame) and call `heyde_animation_tick(server, dt)`.
    - The tick function should iterate active animations, update them, and apply results to corresponding `wlr_scene_node` positions.
  </action>
  <verify>
    <automated>make && ./heyde -v</automated>
  </verify>
  <done>Compositor tracks time and updates animation state every frame.</done>
</task>

<task type="auto">
  <name>Task 3: Window Map Transitions</name>
  <files>src/main.c</files>
  <action>
    Apply spring animations to window mapping.
    - In `toplevel_map_handler`, instead of just displaying the window, initialize a spring animation.
    - Example: Start window at `y - 20` and animate to `y` using a bouncy spring.
    - Ensure `wlr_scene_node_set_position` is used during the animation ticks.
  </action>
  <verify>
    <automated>make && ./heyde -v</automated>
  </verify>
  <done>New windows slide smoothly into position when opened.</done>
</task>

</tasks>

<verification>
1. Launch heyDE.
2. Open a terminal (e.g., foot).
3. Observe the window "sliding" or "popping" into place with a slight bounce.
</verification>

<success_criteria>
- Spring solver correctly converges to target value.
- Frame loop overhead is minimal.
- Window transitions are noticeably smoother than static mapping.
</success_criteria>

<output>
After completion, create `.planning/phases/05-animations-polish/05-01-SUMMARY.md`
</output>
