---
phase: 05-animations-polish
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified: [src/server.h, src/main.c, src/workspace.h, src/workspace.c]
autonomous: true
requirements: [GEST-02]

must_haves:
  truths:
    - "Three-finger swipe on touchpad/mouse triggers workspace change"
    - "Workspace transitions slide horizontally"
  artifacts:
    - path: "src/workspace.h"
      provides: "Workspace management structures"
    - path: "src/workspace.c"
      provides: "Implementation of workspace switching"
  key_links:
    - from: "src/main.c (server_cursor_motion_handler)"
    - to: "src/workspace.c (heyde_workspace_swipe)"
    - via: "wlr_cursor gesture signals"
---

<objective>
Implement workspace management and 3-finger swipe gestures for fluid multi-tasking.

Purpose: Enable spatial workspace navigation.
Output: `src/workspace.h/c` modules and integrated gesture detection.
</objective>

<execution_context>
@C:/Users/hamza/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/hamza/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/server.h
@src/main.c
@.planning/phases/05-animations-polish/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Workspace Infrastructure</name>
  <files>src/workspace.h, src/workspace.c, src/server.h, src/main.c, Makefile</files>
  <action>
    Introduce workspace abstraction.
    - Create `struct heyde_workspace` that wraps a `wlr_scene_tree`.
    - Update `heyde_server` to maintain a list of workspaces (e.g., 9 workspaces).
    - Migrate existing toplevel creation to the current active workspace.
    - Update Makefile to include new files.
  </action>
  <verify>
    <automated>make && ./heyde --help</automated>
  </verify>
  <done>Compositor supports multiple virtual desktops via scene-graph trees.</done>
</task>

<task type="auto">
  <name>Task 2: 3-Finger Gesture Detection</name>
  <files>src/main.c</files>
  <action>
    Handle libinput swipe gestures through `wlr_cursor`.
    - Add listeners for `swipe_begin`, `swipe_update`, and `swipe_end` on `server->cursor->events`.
    - Detect 3-finger horizontal swipes.
    - Call `heyde_workspace_swipe(server, delta_x)` during swipe updates.
  </action>
  <verify>
    <automated>make && ./heyde -v</automated>
  </verify>
  <done>Compositor recognizes 3-finger swipes and passes delta values.</done>
</task>

<task type="auto">
  <name>Task 3: Smooth Workspace Transitions</name>
  <files>src/workspace.c</files>
  <action>
    Implement horizontal sliding transitions.
    - Use the animation system from Plan 01 to animate the workspace root `wlr_scene_tree` nodes.
    - During a swipe, manually offset the trees.
    - On `swipe_end`, use a spring animation to snap to the nearest workspace (or return if swipe was small).
  </action>
  <verify>
    <automated>make && ./heyde -v</automated>
  </verify>
  <done>Workspaces slide horizontally with physics-based snapping.</done>
</task>

</tasks>

<verification>
1. Launch heyDE.
2. Open apps in Workspace 1.
3. Perform a 3-finger horizontal swipe on a touchpad.
4. Observe Workspace 1 sliding out and Workspace 2 sliding in.
</verification>

<success_criteria>
- 3-finger swipes reliably trigger workspace changes.
- Transition feels responsive and follows the user's finger during the swipe.
- Snap-to-grid behavior uses spring physics for a smooth stop.
</success_criteria>

<output>
After completion, create `.planning/phases/05-animations-polish/05-02-SUMMARY.md`
</output>
