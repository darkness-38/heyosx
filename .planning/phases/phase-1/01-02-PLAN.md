---
phase: 01-scaffolding-core-server
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified: [src/main.c]
autonomous: false
requirements: [1.4]

must_haves:
  truths:
    - "The compositor handles SIGINT and SIGTERM signals."
    - "The compositor exits gracefully when Ctrl+C is pressed."
    - "No crashes or memory leaks reported on clean shutdown."
  artifacts:
    - path: "src/main.c"
      provides: "Signal handling logic"
      contains: "wl_event_loop_add_signal"
  key_links:
    - from: "SIGINT/SIGTERM"
      to: "wl_display_terminate"
      via: "signal handler callback"
---

<objective>
Implement graceful shutdown and signal handling for the core server.
</objective>

<execution_context>
@C:/Users/hamza/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/hamza/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/phase-1/01-01-SUMMARY.md

# Interfaces (Anticipated wlroots/Wayland exports)
- wl_event_loop_add_signal: Registers a handler for system signals.
- wl_display_terminate: Stops the event loop.
- wl_display_destroy_clients: Cleanly disconnects all clients.
- wl_display_destroy: Frees the Wayland display resources.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add signal handling to main.c</name>
  <files>src/main.c</files>
  <action>
    Add logic to catch SIGINT and SIGTERM:
    1. Get the event loop using `wl_display_get_event_loop(server.wl_display)`.
    2. Define a handler function that calls `wl_display_terminate(server->wl_display)`.
    3. Use `wl_event_loop_add_signal(event_loop, SIGINT, handle_signal, &server)` to register for SIGINT.
    4. Use `wl_event_loop_add_signal(event_loop, SIGTERM, handle_signal, &server)` to register for SIGTERM.
  </action>
  <verify>
    <automated>grep "wl_event_loop_add_signal" src/main.c</automated>
  </verify>
  <done>SIGINT and SIGTERM are handled via the Wayland event loop.</done>
</task>

<task type="auto">
  <name>Task 2: Implement cleanup sequence in main.c</name>
  <files>src/main.c</files>
  <action>
    After the event loop (`wl_display_run`) finishes:
    1. Call `wl_display_destroy_clients(server.wl_display)`.
    2. Free created resources (renderer, allocator, scene, layout, display).
    3. Ensure no resources are leaked according to wlroots' standard cleanup practices.
  </action>
  <verify>
    <automated>grep "wl_display_destroy_clients" src/main.c</automated>
  </verify>
  <done>Clean shutdown logic is implemented after the event loop terminates.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Compositor with core initialization and signal handling.</what-built>
  <how-to-verify>
    1. Run `make`.
    2. Run `./heyde`.
    3. Ensure a window (backend) opens (if running in Wayland/X11 environment).
    4. Press Ctrl+C in the terminal.
    5. Verify the compositor exits gracefully without crashing.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
Manual test with Ctrl+C and automated check for cleanup function presence.
</verification>

<success_criteria>
- `heyde` binary handles Ctrl+C and exits cleanly.
- Build system produces a functional, signal-aware compositor base.
</success_criteria>

<output>
After completion, create `.planning/phases/phase-1/01-02-SUMMARY.md`
</output>
