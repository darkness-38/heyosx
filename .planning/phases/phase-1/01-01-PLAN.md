---
phase: 01-scaffolding-core-server
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/server.h, src/main.c, Makefile]
autonomous: true
requirements: [1.1, 1.2, 1.3]

must_haves:
  truths:
    - "The project compiles without errors."
    - "The server initializes wlroots without crashing."
    - "An empty wlroots backend (e.g., Wayland or X11 nested window) is displayed."
  artifacts:
    - path: "src/server.h"
      provides: "Compositor state structure"
      contains: "struct heyde_server"
    - path: "src/main.c"
      provides: "Main entry point and event loop"
      contains: "wl_display_create"
    - path: "Makefile"
      provides: "Build system for the compositor"
      contains: "pkg-config"
  key_links:
    - from: "src/main.c"
      to: "src/server.h"
      via: "include"
    - from: "Makefile"
      to: "src/main.c"
      via: "compilation target"
---

<objective>
Initialize the base wlroots environment by defining the core server state, implementing the main event loop, and setting up the build system.
</objective>

<execution_context>
@C:/Users/hamza/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/hamza/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Interfaces (Anticipated wlroots exports)
- wl_display_create: Creates a Wayland display.
- wlr_backend_autocreate: Creates a backend (DRM, Wayland, X11).
- wlr_renderer_autocreate: Creates a GLES2 renderer.
- wlr_allocator_autocreate: Creates a buffer allocator.
- wlr_scene_create: Creates a scene graph.
- wlr_output_layout_create: Manages output placement.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define core server state in server.h</name>
  <files>src/server.h</files>
  <action>
    Create src/server.h defining the `heyde_server` structure.
    Include necessary wlroots headers:
    - <wayland-server-core.h>
    - <wlr/backend.h>
    - <wlr/render/allocator.h>
    - <wlr/render/wlr_renderer.h>
    - <wlr/types/wlr_scene.h>
    - <wlr/types/wlr_output_layout.h>

    Structure `heyde_server` must include:
    - struct wl_display *wl_display;
    - struct wlr_backend *backend;
    - struct wlr_renderer *renderer;
    - struct wlr_allocator *allocator;
    - struct wlr_scene *scene;
    - struct wlr_output_layout *output_layout;
  </action>
  <verify>
    <automated>test -f src/server.h</automated>
  </verify>
  <done>src/server.h exists with the correct state structure.</done>
</task>

<task type="auto">
  <name>Task 2: Implement main.c with wlroots initialization</name>
  <files>src/main.c</files>
  <action>
    Implement src/main.c to initialize the compositor:
    1. Include "server.h".
    2. Instantiate `struct heyde_server`.
    3. Initialize display: `wl_display_create()`.
    4. Initialize backend: `wlr_backend_autocreate()`.
    5. Initialize renderer: `wlr_renderer_autocreate()`.
    6. Initialize allocator: `wlr_allocator_autocreate()`.
    7. Initialize scene graph: `wlr_scene_create()`.
    8. Initialize output layout: `wlr_output_layout_create()`.
    9. Call `wlr_backend_start(server.backend)`.
    10. Run event loop: `wl_display_run(server.wl_display)`.
    
    Add minimal error handling for each initialization step.
  </action>
  <verify>
    <automated>test -f src/main.c</automated>
  </verify>
  <done>src/main.c is implemented with full wlroots initialization flow.</done>
</task>

<task type="auto">
  <name>Task 3: Create Makefile and verify build</name>
  <files>Makefile</files>
  <action>
    Create a Makefile that uses `pkg-config` to find dependencies for `wlroots-0.18`, `wayland-server`, and `xkbcommon`.
    - Compiler: gcc
    - CFLAGS: -Wall -Wextra -Werror -g `pkg-config --cflags wlroots-0.18 wayland-server xkbcommon`
    - LIBS: `pkg-config --libs wlroots-0.18 wayland-server xkbcommon`
    - Target: heyde (from src/main.c)

    Perform a test build to ensure everything compiles.
  </action>
  <verify>
    <automated>make && ./heyde --help || true</automated>
  </verify>
  <done>Makefile exists and successfully compiles the `heyde` binary.</done>
</task>

</tasks>

<verification>
Check if `heyde` binary exists and runs.
Verify using `wl-display` check if possible, or manual visual check in next plan.
</verification>

<success_criteria>
- Makefile produces `heyde` binary.
- `heyde` runs and initializes a wlroots backend.
</success_criteria>

<output>
After completion, create `.planning/phases/phase-1/01-01-SUMMARY.md`
</output>
