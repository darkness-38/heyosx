---
phase: 03-layer-shell-input
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/server.h, src/main.c]
autonomous: true
requirements: [3.2]

must_haves:
  truths:
    - "The seat and cursor are correctly initialized."
    - "Input devices (mouse/keyboard) are handled."
  artifacts:
    - path: "src/server.h"
      provides: "Input state"
      contains: "struct heyde_keyboard"
    - path: "src/main.c"
      provides: "Input handlers"
      contains: "server_new_input"
---

<objective>
Initialize input handling infrastructure (seat, cursor, keyboard).
</objective>

<execution_context>
@C:/Users/hamza/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Update server.h for Input</name>
  <files>src/server.h</files>
  <action>
    Include input-related headers:
    - <wlr/types/wlr_cursor.h>
    - <wlr/types/wlr_xcursor_manager.h>
    - <wlr/types/wlr_seat.h>
    - <wlr/types/wlr_keyboard.h>
    
    Add struct heyde_keyboard.
    Update heyde_server with:
    - struct wlr_cursor *cursor;
    - struct wlr_xcursor_manager *cursor_mgr;
    - struct wlr_seat *seat;
    - struct wl_list keyboards;
    - listeners for new_input, cursor_motion, cursor_button, etc.
  </action>
</task>

<task type="auto">
  <name>Task 2: Implement Input Handling in main.c</name>
  <files>src/main.c</files>
  <action>
    Initialize wlr_cursor, wlr_xcursor_manager, and wlr_seat in main.
    Implement server_new_input handler.
    Implement handlers for cursor motion, button, axis, and frame events.
    Ensure cursor is attached to output layout.
  </action>
</task>

</tasks>

<success_criteria>
- Build successful in WSL.
- Cursor can move across outputs.
- Keyboard devices are detected.
</success_criteria>
