---
phase: 03-layer-shell-input
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [Makefile, src/server.h, src/main.c]
autonomous: true
requirements: [3.1]

must_haves:
  truths:
    - "Layer-shell protocol is initialized."
    - "Layer surfaces are correctly added to the scene graph layers."
  artifacts:
    - path: "src/server.h"
      provides: "Layer surface state"
      contains: "struct heyde_layer_surface"
    - path: "src/main.c"
      provides: "Layer surface handler"
      contains: "server_new_layer_shell_surface"
---

<objective>
Implement support for the Layer-Shell protocol to allow UI elements like bars.
</objective>

<execution_context>
@C:/Users/hamza/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Update Makefile for Layer-Shell</name>
  <files>Makefile</files>
  <action>
    Add layer-shell protocol generation to Makefile.
    XDG_SHELL_XML already exists. Add:
    LAYER_SHELL_XML = /usr/share/wlr-protocols/unstable/wlr-layer-shell-unstable-v1.xml
    Generate layer-shell-protocol.h/c.
    Add to SRC and OBJ.
  </action>
</task>

<task type="auto">
  <name>Task 2: Update server.h for Layer-Shell</name>
  <files>src/server.h</files>
  <action>
    Add struct heyde_layer_surface.
    Include <wlr/types/wlr_layer_shell_v1.h>.
    Update heyde_server with wlr_layer_shell_v1 and new_layer_shell_surface listener.
  </action>
</task>

<task type="auto">
  <name>Task 3: Implement Layer-Shell in main.c</name>
  <files>src/main.c</files>
  <action>
    Initialize wlr_layer_shell_v1 in main.
    Implement server_new_layer_shell_surface handler.
    Add map/unmap/destroy listeners for layer surfaces.
    Correctly place layer surfaces in scene graph layers based on their requested layer.
  </action>
</task>

</tasks>

<success_criteria>
- Build successful in WSL.
- Layer-shell applications can connect and display.
</success_criteria>
