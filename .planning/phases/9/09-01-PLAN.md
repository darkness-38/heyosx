---
phase: 09-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [heydm/src/render.rs, heydm/src/panel.rs]
autonomous: true
requirements: [FEAT-UI-01, FEAT-UI-02]

must_haves:
  truths:
    - "A real-time clock is visible in the status bar"
    - "Active workspace is visually highlighted in the bar"
  artifacts:
    - path: "heydm/src/render.rs"
      provides: "fontdue text rasterization and status bar drawing"
---

<objective>
Implement high-fidelity text rendering and a dynamic status bar using fontdue and tiny-skia.

Purpose: Provide essential system information and workspace feedback with a premium aesthetic.
Output: Dynamic clock, battery status, and workspace indicators.
</objective>

<execution_context>
@C:/Users/hamza/.gemini/get-shit-done/workflows/execute-plan.md
@C:/Users/hamza/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@heydm/src/render.rs
@heydm/src/panel.rs
@heydm/src/window.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup Font Loading in Renderer</name>
  <files>heydm/src/render.rs</files>
  <action>
    - Add `font: fontdue::Font` field to `Renderer` (or load it statically/lazily).
    - Load `/usr/share/fonts/TTF/DejaVuSans.ttf` or `/usr/share/fonts/noto/NotoSans-Regular.ttf` into memory.
    - Initialize `fontdue::Font::from_bytes`.
  </action>
  <verify>
    <automated>cargo check</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Implement draw_text helper</name>
  <files>heydm/src/render.rs</files>
  <action>
    - Implement `fn draw_text(pixmap: &mut Pixmap, font: &Font, text: &str, x: f32, y: f32, size: f32, color: Color)`.
    - Loop through characters, rasterize with `font.rasterize`.
    - Blit the alpha map into the `pixmap` using `paint.set_color`.
  </action>
  <verify>
    <automated>cargo check</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Update draw_ui with real status info</name>
  <files>heydm/src/render.rs</files>
  <action>
    - Replace placeholder pills with real status info.
    - Render the clock string from `state.panel.clock_text()`.
    - Render battery and network info.
    - Render workspace indicators: 9 small pills, highlighting `state.window_manager.current_workspace`.
  </action>
  <verify>
    <automated>cargo check</automated>
  </verify>
</task>

</tasks>

<verification>
1. `cargo check` passes.
2. Build ISO and boot.
3. Verify the top bar shows the current time and active workspace.
</verification>

<success_criteria>
- Text is legible and correctly positioned.
- Clock updates every second.
- Workspace switching is reflected in the UI immediately.
</success_criteria>

<output>
After completion, create `.planning/phases/9/09-01-SUMMARY.md`
</output>
