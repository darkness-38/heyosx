---
phase: 02-output-xdg-shell
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified: [src/server.h, src/main.c]
autonomous: true
requirements: [2.2, 2.3]

must_haves:
  truths:
    - "The xdg-shell protocol is initialized."
    - "New application windows (xdg-shell toplevels) are detected."
    - "Application windows are added to the scene graph."
  artifacts:
    - path: "src/server.h"
      provides: "Toplevel state structure"
      contains: "struct heyde_toplevel"
    - path: "src/main.c"
      provides: "New surface handler"
      contains: "server_new_xdg_surface"
---

<objective>
Implement XDG-shell integration for application windows and basic management.
</objective>

<execution_context>
@C:/Users/hamza/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Update server.h for XDG-shell</name>
  <files>src/server.h</files>
  <action>
    Add `struct heyde_toplevel` to `src/server.h`.
    Include <wlr/types/wlr_xdg_shell.h>.
    
    `struct heyde_toplevel` must include:
    - struct wl_list link;
    - struct heyde_server *server;
    - struct wlr_xdg_toplevel *xdg_toplevel;
    - struct wlr_scene_tree *scene_tree;
    - struct wl_listener map;
    - struct wl_listener unmap;
    - struct wl_listener destroy;
    - struct wl_listener request_move;
    - struct wl_listener request_resize;
    - struct wl_listener request_maximize;
    - struct wl_listener request_fullscreen;

    Update `heyde_server` to include:
    - struct wlr_xdg_shell *xdg_shell;
    - struct wl_list toplevels;
    - struct wl_listener new_xdg_surface;
  </action>
  <verify>
    <automated>grep "struct heyde_toplevel" src/server.h</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Implement XDG-shell handling in main.c</name>
  <files>src/main.c</files>
  <action>
    Implement XDG-shell logic:
    1. Define handlers for `map`, `unmap`, `destroy`, `request_move`, etc.
    2. Define `server_new_xdg_surface_handler`.
    3. In `server_new_xdg_surface_handler`:
       - Handle `WLR_XDG_SURFACE_ROLE_TOPLEVEL`.
       - Create `heyde_toplevel`.
       - Add to `server->scene` using `wlr_scene_xdg_surface_create`.
       - Initialize listeners.
    4. In `main()`:
       - Initialize `server.toplevels` list.
       - Create `xdg_shell`: `wlr_xdg_shell_create(server.wl_display, 6)`.
       - Connect `new_xdg_surface` listener.
  </action>
  <verify>
    <automated>grep "server_new_xdg_surface" src/main.c</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- XDG-shell applications can connect and their windows are displayed.
- Build successful.
</success_criteria>
