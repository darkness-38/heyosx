---
phase: 02-output-xdg-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/server.h, src/main.c]
autonomous: true
requirements: [2.1]

must_haves:
  truths:
    - "The compositor correctly identifies new outputs."
    - "Each output is added to the output layout."
    - "Each output is enabled and starts rendering."
  artifacts:
    - path: "src/server.h"
      provides: "Output state structure"
      contains: "struct heyde_output"
    - path: "src/main.c"
      provides: "New output handler"
      contains: "server_new_output"
---

<objective>
Implement output layout management and handling of new outputs.
</objective>

<execution_context>
@C:/Users/hamza/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Update server.h for output management</name>
  <files>src/server.h</files>
  <action>
    Add `struct heyde_output` to `src/server.h`.
    Include <wlr/types/wlr_output.h>.
    
    `struct heyde_output` must include:
    - struct wl_list link;
    - struct heyde_server *server;
    - struct wlr_output *wlr_output;
    - struct wl_listener frame;
    - struct wl_listener request_state;
    - struct wl_listener destroy;

    Update `heyde_server` to include:
    - struct wl_list outputs; (initialize in main)
    - struct wl_listener new_output;
  </action>
  <verify>
    <automated>grep "struct heyde_output" src/server.h</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Implement output handling in main.c</name>
  <files>src/main.c</files>
  <action>
    Implement output handling logic:
    1. Define `output_frame_handler`, `output_request_state_handler`, and `output_destroy_handler`.
    2. Define `server_new_output_handler`.
    3. In `server_new_output_handler`:
       - Use `wlr_output_layout_add_auto(server->output_layout, wlr_output)`.
       - Initialize listeners for the output.
       - Use `wlr_output_create_global(wlr_output)`.
       - Add to `server->outputs` list.
    4. In `main()`:
       - Initialize `server.outputs` list.
       - Connect `new_output` listener to `server.backend->events.new_output`.
  </action>
  <verify>
    <automated>grep "server_new_output" src/main.c</automated>
  </verify>
</task>

</tasks>

<success_criteria>
- Compositor detects outputs and adds them to the layout.
- Build successful.
</success_criteria>
